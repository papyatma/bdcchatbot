<p id="notice"><%= notice %></p>

<h1>Seances</h1>

<table>
  <thead>
    <tr>
      <th>Public</th>
      <th>Agemin</th>
      <th>Agemax</th>
      <th>Niveau</th>
      <th>Sexe</th>
      <th>Periode</th>
      <th>Jour</th>
      <th>Heuredebut</th>
      <th>Heurefin</th>
      <th>Datedebut</th>
      <th>Datefin</th>
      <th>Tarif</th>
      <th>Place</th>
      <th>Activité</th>
      <th>Structure</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @seances.each do |seance| %>
      <tr>
        <td><%= seance.public %></td>
        <td><%= seance.ageMin %></td>
        <td><%= seance.ageMax %></td>
        <td><%= seance.niveau %></td>
        <td><%= seance.sexe %></td>
        <td><%= seance.periode %></td>
        <td><%= seance.jour %></td>
        <td><%= seance.heureDebut %></td>
        <td><%= seance.heureFin %></td>
        <td><%= seance.dateDebut %></td>
        <td><%= seance.dateFin %></td>
        <td><%= seance.tarif %></td>
        <td><%= seance.place_id %></td>
        <td><%= seance.activite_id%></td>
        <td><%= seance.structure_id %></td>
        <td><%= link_to 'Show', seance %></td>
        <td><%= link_to 'Edit', edit_seance_path(seance) %></td>
        <td><%= link_to 'Destroy', seance, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Seance', new_seance_path %>
